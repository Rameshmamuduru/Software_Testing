- **Stub:** if any child module not available to test another child module a temporary code can be used in between the two modules to test those two modules that temp code is called "stub" ---- **in Top-down approach**
- **Driver:** if any child module not available to test another child module a temporary code can be used in between the two modules to test those two modules that temp code is called "stub" ---- **in bottom up approach**

